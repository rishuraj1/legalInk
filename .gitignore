# Root-level ignores (common across both frontend and backend)
node_modules/
bun.lockb
.env
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.pem
*.env.local

# Logs and OS generated files
logs/
*.log
*.lock

# Ignore specific editor files
.vscode/
.idea/
*.swp

# Ignore build output folders
dist/
build/

# Ignore coverage and test output
coverage/
test-output/

# Ignore frontend (Next.js app with Bun runtime)
frontend/.next/           # Next.js build output
frontend/node_modules/    # Node modules in frontend (Bun will use bun.lockb instead)
frontend/bun.lockb        # Bun lock file
frontend/.env.local       # Local environment variables in frontend
frontend/.env.development.local # Local development environment variables
frontend/.env.test.local  # Local test environment variables
frontend/.env.production.local # Local production environment variables
frontend/out/             # Next.js static export directory
frontend/.turbo/          # Turbopack cache (if using)
frontend/.vercel/         # Vercel deployment cache (if using Vercel)

# Ignore backend (Express.js app with Bun runtime)
backend/node_modules/     # Node modules in backend (Bun will use bun.lockb instead)
backend/bun.lockb         # Bun lock file for backend
backend/.env              # Environment variables in backend
backend/dist/             # Transpiled/compiled output (if using TypeScript)
backend/uploads/          # Uploads folder, if handling file uploads in the backend
backend/coverage/         # Test coverage output (if using testing frameworks)

# Bun cache and development artifacts
bun-manifest.json         # Bun manifest file
.cache/                   # Bun cache files
